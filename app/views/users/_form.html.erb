<%= form_for(@user) do |f| %>

  <%= render 'shared/error_messages', object: f.object %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= render 'shared/password_fields', f: f %>

  <%= label_tag do %>
	  <%= f.check_box :follower_notification %>
	  Email me when I'm followed by someone new
  <% end %>

  <%= f.submit btn_txt, class: "btn btn-large btn-primary" %>

<% end %>
